# 23 — Bitácora de sesión 2026-07-17 (archivo del avance)

> Cierre y archivo de una sesión larga. Deja el estado exacto para retomar sin
> re-investigar. El plan de lo que sigue vive en `docs/24-IVI-STUDIO-PLAN.md`.

## Qué quedó en producción (geógrafo 100.117.204.80:8080)

El operador corrió `deploy-ivi-geografo.sh` (9 pasos verdes, kill -9 revivido
en vivo). **Prod = main.** Todo lo de esta sesión está desplegado:

| Corte | Commit | Qué cambió | Verificado |
|---|---|---|---|
| P0 fundaciones | `37a8ba3` | systemd, `/api/health`, sesión por navegador, `_SESSIONS` thread-safe | kill -9 → revive; sids en log |
| Materialidad ROAS | `8175295` | 4 consumidores dejan de coronar polvo (Honduras USD 33); renderer `md()` real (+fix XSS) | E2E: respuesta sin polvo |
| Higiene de países | `54fac50` | `paisDesdeIso`, `nombreBonito`, filtro 0/0 | 224/224 TS |
| Formato adaptativo | `8a6b079` | corto conversacional (default) / informe (a pedido), lo decide el motor | 780ch/13,9s vs 2.400/30s |
| Corroboración + skills | `b81b83f` | 9/9 cifras exactas vs SQL; 13 marketing skills + `product-marketing.md` | SQL directo |
| Lazo CAPI | `0b98507` | apagado explícito (`LAZO_RELOJ=`); campo `reloj` en el payload | `reloj: apagado` en vivo |
| P1+P2 (instantáneo) | `0ecb8fa` | caché por huella (sha256 del prompt) + warmer de las 12 | 12/12 botones ≤19ms |
| Ivi Studio (proto) | `d2f44ed` | flujo supervisado paso a paso, 5 pasos, datos reales | Playwright desktop+móvil |

Suite del engine: **77/77** (`python3 goberna-kos/tests/run.py`). TS: 224/224.

## Hallazgos que cambian decisiones

1. **El lazo CAPI nunca estuvo en cron**: las 107 Purchase del 13/07 fueron una
   corrida manual. Ya estaba fail-closed; ahora apagado explícito por decisión
   de Estephano. El monto real de las 273 ventas sin reportar es **USD 32.926**
   (la estimación de Ivi con ticket-mix, 21.111, estaba ~35% baja).
2. **El doble cerebro mordió en vivo**: el min/max naive de ROAS estaba repetido
   en 4 lugares (impacto, anomalías, insight, prompt). Un solo `roas_material()`
   los unificó. Evidencia perfecta para el fix K1 de docs/21 (métrica en SQL).
3. **La deuda de frescura**: dump de Cerberus al 11/07 (6 días). Acorta el valor
   de todo. Y no hay serie de gasto POR PAÍS (la atribución es una foto).

## Loops de marketing en marcha (docs/22)

- **Loop 1 (pauta)** corrida #1 (`01a9ca3`): +USD 3.000 en pasos de +20% hacia
  un techo de 11.290. Ejecuta Estephano en Ads Manager.
- **Loop 3 (CAC)** corrida #1 (`c9dff38`): línea base 8 semanas, CAC blended
  11,8→18,3 mientras el gasto subía 80%. Freno: CAC>24 dos semanas seguidas.
- Corridas #2: **lunes 2026-07-20**, dependen del dump nuevo de Cerberus.

## Prototipo Ivi Studio

`docs/prototypes/ivi-studio.html` (self-contained). Se sirve con
`python3 -m http.server 8123 -d docs/prototypes` → `http://127.0.0.1:8123/ivi-studio.html`
(file:// bloqueado). Es la **v1**: prueba la UX del flujo guiado, con imágenes
picsum y textos pre-escritos. La v2 (adaptativa, con chat y texto libre) se
diseña en docs/24.

## Decisiones abiertas (heredadas)

- **P3** (motor que piensa: hipótesis/acciones reales) y **P4** (des-resumir) de
  docs/19 — el techo de calidad que sigue.
- **K0** (nacimiento del kernel Gen-2) de docs/21.
- **Ivi Studio v2** — el foco nuevo, planeado en docs/24.
- Persistir `gastoPorPais` semanal (habilita el detector CAC por país).
- Acortar el ciclo de dump de Cerberus (frescura).

## Cómo levantar todo para retomar

```
docker compose up -d --wait                 # Postgres :5434 (datos)
cd server && npm run dev                     # backend :4100
# Ivi local contra modelo real de geógrafo (Tailscale up primero):
python3 goberna-kos/ivi/... (ver scratchpad ivi_p0_local.py)
# Prototipo Studio:
python3 -m http.server 8123 -d docs/prototypes
```
Gotcha operativo: si geógrafo "no responde", revisar `Tailscale status` primero.
