# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files (secrets — never commit)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Salidas de herramientas locales
.playwright-mcp/
*.png

# Artefactos de build de TS
*.tsbuildinfo

# Python (el engine de Ivi)
__pycache__/
*.pyc

# Salidas generadas del CQ Engine. La fuente (cqs/catalog.json, cqs/capabilities.json) SÍ se
# versiona; los benchmarks se derivan de ella con `kos cq export` y llevan timestamp en el nombre.
goberna-kos/cqs/benchmarks/
answers-cache.json*
